// sex_imports.dol - Test WASM imports from sex fun without body

// Host imports (no body = WASM import from "loa" module)
sex fun vudo_print(ptr: Int32, len: Int32)
sex fun vudo_alloc(size: Int32) -> Int32

// Pure function - has body, gets exported
fun add(a: Int64, b: Int64) -> Int64 {
    return a + b
}

// Sex function with body - also exported
sex fun compute(x: Int64) -> Int64 {
    return x * 2
}
